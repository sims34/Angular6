import { SoftLanguageDirective } from './soft-language.directive';

describe('SoftLanguageDirective', () => {
  it('should create an instance', () => {
    const directive = new SoftLanguageDirective();
    expect(directive).toBeTruthy();
  });
});
